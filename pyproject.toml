[project]
name = "skyscenes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "opencv-python",
    "tqdm",
    "pyyaml",
    "setuptools>=75.3.4",
    "pillow>=10.4.0",
]
